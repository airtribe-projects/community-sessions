package org.example.enums;

/**
 * Represents the type of elevator request
 */
public enum RequestType {
    HALL_REQUEST,       // External request from floor hall button
    ELEVATOR_REQUEST    // Internal request from inside elevator
}
